# 代码优化

持续发现系统中的优化点并自我更新改进。

## 用法

```bash
/optimize
```

或者直接与任务一起执行 - 系统会自动检查优化点。

## 已完成的优化

### 1. feed.ts 路由优化 (已完成)
- 使用 map + concat 替代 forEach + push
- 添加类型定义 (FeedItem, DraftNote)
- 移除 any 类型，使用强类型
- 添加 limit 保护（最大100）
- 添加错误日志记录

### 2. Editor.tsx 类型优化 (已完成)
- 移除 2 处 as any 类型转换
- 使用 NoteCreate 类型

### 3. analytics.ts 优化 (已完成)
- 添加类型定义 (ActivityRow, TagRow, CategoryRow, Last30DaysStats)
- 移除 4 处 any 类型，使用强类型
- 添加 limit 保护
- 添加 5 处错误日志记录

### 4. graph.ts 优化 (已完成)
- 添加类型定义 (GraphNode, GraphLink, ApiNode, ApiLink)
- 移除所有 11 处 any 类型，使用强类型
- 添加 10 处错误日志记录

### 5. 测试套件 (已完成)
- 创建 `apps/api/src/test/optimization.test.ts`
- 50 个单元测试全部通过
- 覆盖范围：
  - feed.ts 数据转换函数 (6 个测试)
  - analytics.ts 类型定义 (4 个测试)
  - graph.ts 类型定义 (2 个测试)
  - 数据处理逻辑 (5 个测试)
  - 字符串处理 (4 个测试)
  - 输入验证 (5 个测试)
  - 数学计算 (4 个测试)
  - 对象处理 (3 个测试)
  - 数组处理 (4 个测试)
  - 错误处理 (2 个测试)
  - 边界情况 (11 个测试)

## 优化检查清单

每次处理任务时，必须自动检查以下优化点：

### 1. 代码质量优化
- 检查并移除不必要的 console.log/console.error（生产环境）
- 检查重复代码，提取公共函数
- 检查未使用的变量和导入
- 检查类型安全，避免使用 any

### 2. React/前端优化
- 检查 useEffect 依赖数组是否完整
- 检查是否有不必要的重新渲染（使用 useMemo/useCallback）
- 检查图片和静态资源是否被正确优化

### 3. 后端/性能优化
- 检查数据库查询是否有 N+1 问题
- 检查是否有不必要的循环和重复计算
- 检查 API 是否有适当的错误处理
- 检查是否有内存泄漏风险

### 4. 安全优化
- 检查敏感信息是否暴露在日志中
- 检查输入验证是否充分
- 检查权限控制是否正确

### 5. TypeScript 最佳实践
- 检查是否使用了 strict 模式
- 检查类型定义是否完整
- 检查是否有隐式的 any

## 自我反思问题

每完成一个任务后，必须问自己：

1. 这段代码是否有更优雅的实现方式？
2. 是否有性能可以提升的地方？
3. 是否有重复的代码可以抽象？
4. 是否有潜在的 bug 风险？
5. 代码是否易于维护和扩展？

## 持续改进流程

1. **任务开始前**: 快速浏览相关代码，寻找优化点
2. **编写代码时**: 遵循最佳实践，避免引入新问题
3. **任务完成后**: 回顾代码，识别可改进之处
4. **定期**: 全面审查项目，识别系统性问题

## 运行测试

```bash
cd apps/api
npx tsx --test src/test/optimization.test.ts
```

## 输出格式

当发现优化点时，使用以下格式记录：

```
## 优化点 [编号]

**位置**: 文件路径
**问题**: 描述问题
**建议**: 改进建议
**优先级**: P0/P1/P2
**状态**: pending/in_progress/completed
```
